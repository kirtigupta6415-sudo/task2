import pandas as pd
import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt
df=pd.read_csv("Titanic-Dataset.csv")
df.head()
df=pd.read_csv("Titanic-Dataset.csv")
df.head()

df=pd.read_csv("train.csv")

df.head()
df.shape #check shape of datasets
df.columns
df.info() #give dataset informations
df.describe() #summarizes the statistics
df.describe().round() 
df.isnull().sum() #show missing value
df[df.duplicated()] #show duplicated
#contain zero duplicate value

df["Age"].unique().shape
df["Age"]=df["Age"].fillna(df["Age"].median()) #use median because its a numeric data
df.isnull().sum()
df["Embarked"]=df["Embarked"].fillna(df["Embarked"].mode()[0]) #use mode because it is catorgorical data
df.isnull().sum()
df.drop('Cabin',axis=1,inplace=True)
 #remove cabin column as it has many missing values
print(df.columns)
df["Age"].astype("int64")
df.isnull().sum()
df.duplicated().sum()
df[df.duplicated()]
#now EDA
 sns.countplot=(x="Survived",data=df)
plt.title("Survival count")
plt.show()
import seaborn as sns
import matplotlib.pyplot as plt
import numpy as np
import pandas as pd
sns.countplot(x="Survived",data=df) #survival count
plt.title("Survival Count")
plt.show()
sns.countplot(x="Sex",hue="Survived",data=df)    # which gender survived the most
plt.title("Gender vs Survived")
plt.show() 
sns.countplot(x="Pclass",hue="Survived",data=df)     # which passenger class survived the most
plt.title("Passenger class vs Survived")
plt.show()
plt.hist(df["Age"],bins=20)       # age group present in the titanic
plt.title("Age Distribution")
plt.xlabel("Age")
plt.ylabel("count")
plt.show()
plt.figure(figsize=(10,6))
sns.heatmap(df.corr(numeric_only=True),annot=True,cmap="coolwarm")   #  which suffer positive and negative survival
plt.title("Correlation heatmaps")
plt.show()
sns.boxplot(x="Pclass",y="Fare",data=df)    #  which class cost the most
plt.title("Fare by passenger class")   
plt.show()
sns.countplot(x="Embarked",hue="Survived",data=df)    # which embarked people survived the most
plt.title("Embarked vs Survival")
plt.show()
df["Familysize"]=df["SibSp"]+df["Parch"]    # feature engineering 
df.columns
df.head()

